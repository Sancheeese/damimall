spring:
  application:
    name: damimall-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        namespace: damimall-gateway
        group: dev

